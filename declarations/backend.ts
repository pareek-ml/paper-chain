// frontend/declarations/backend.ts
// Minimal stub so frontend can compile.
// Replace with real DFX-generated declarations later.

export const canisterId = "aaaaa-aa"; // placeholder, not a real canister

// Vite/TS only cares that this exists and is callable.
// At runtime, this won't expose real methods.
export const idlFactory = (_: any) => {
  return {} as any;
};